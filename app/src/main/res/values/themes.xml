<resources>
    <!-- Base application theme. -->
    <style name="Theme.WellnessAppProt" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Transparent</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/Transparent</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Estilo personalizado para el Spinner -->
    <style name="SpinnerStyle">
        <item name="android:background">#00AF91</item>
        <item name="android:padding">8dp</item>
        <item name="android:layout_width">10dp</item>
        <item name="android:layout_height">5dp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="CustomToolBar" parent="Widget.AppCompat.Toolbar">
        <!-- Otros atributos personalizados para la Toolbar -->

        <!-- Cambiar el color del icono de las tres líneas -->
        <item name="android:textColorSecondary">#FFFFFF</item>
    </style>
    
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>